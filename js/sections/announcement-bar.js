import svelteWrapper from "JsComponents/svelte-wrapper";
import AnnouncementBar from 'SvelteComponents/AnnouncementBar.svelte';

export default( ) => {
    svelteWrapper(AnnouncementBar,'announcement-bar')
}
