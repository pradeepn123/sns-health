{% comment %} Disable boost search {% endcomment %}
{%- assign enable_boost_search = true -%}
{%- if enable_boost_search -%}
	{%- render 'search-template-boost' -%}
{%- else -%}
	{%- render 'search-template-shopify' -%}
{%- endif -%}